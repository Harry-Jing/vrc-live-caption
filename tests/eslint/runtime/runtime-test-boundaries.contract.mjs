// Runtime tests follow the same adapter seam as runtime production modules.

// eslint-disable-next-line no-restricted-imports -- Runtime tests must not import Preview planning helpers
import "../../../src/preview/runtimeBackend.ts";
